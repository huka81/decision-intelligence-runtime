# Version

The Python distribution identifier and semantic version live in the repository root `pyproject.toml` under `[project]` (`name` and `version` for the `dir-runtime` package). Bump those fields when you cut a release—this page only points readers at the canonical source in the repo.
