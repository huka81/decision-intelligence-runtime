# Mocks — `09_topology_a_eoam`

- **`quote_generator.py`** — same multiplicative random-walk tick stream as sample `31_finance_trading`; used to build one `OBSERVATION` payload per demo run.
- **`market_events.py`** — `QuoteTick` / `NewsEvent` models shared with that generator.
