"""Sample 33 — insurance underwriting (Topology C). Optional: load dotenv if present."""

from __future__ import annotations

try:
    from dotenv import load_dotenv

    load_dotenv()
except ImportError:
    pass
